<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WOSoMP : Live</title>
    <%= csrf_meta_tags %>

    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Live Stylesheet -->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <link href="/favicon.ico" rel="shortcut icon">

    <%= render "layouts/analytics" %>

    <!-- Head content below this comment inserted with yield:head hook -->
    <%= yield :head %>
  </head>
  <body class="live">


    <div id="nav-wrapper" class="inside">
      <div class="container">
        <a href="<%=live_path()-%>"><h1 id="logo" class="inside"><span>WOSoMP 2013</span></h1></a>
        <ul id="top-nav" class="row">
          <%= top_nav_item("Teams", teams_live_path()) -%>
          <%= top_nav_item("Athletes", athletes_live_path()) -%>
          <%= top_nav_item("Schedule", schedule_live_path()) -%>
          <%= top_nav_item("#wosomp", media_live_path()) -%>
        </ul><!--/.row-->
      </div><!--/.container-->
    </div>

    <article id="content">
      <%= yield %>
    </article> <!--/#content-->

    <!-- Major JavaScript Injection: -->
    <%= javascript_include_tag "application" %>

    <!-- Javascript content below this comment inserted with yield:javascripts hook -->
    <%= yield :javascripts %>
  </body>
</html>
